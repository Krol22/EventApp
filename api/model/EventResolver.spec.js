describe('Event resolver', () => {
    it('should be defined', () => {

    });

    describe('get events', () => {
        it('should find all events', () => {

        });
    });

    describe('addEvent', () => {
        it('should return new event after save', () => {

        });

        it('should throw custom error if firstName, lastName, email or eventDate is not presented', () => {

        });

        it('should throw custom error when email is not valid', () => {

        });

        it('should throw custom error when mongoose save falis', () => {

        });
    });
});
